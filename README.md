# Nodeverse Learn

## Project info

This is a Vite + React + TypeScript + shadcn-ui + Tailwind CSS application.

## Running the project locally

The only requirement is having Node.js & npm installed - [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating)

Follow these steps:

```sh
# Step 1: Clone the repository using the project's Git URL.
git clone <YOUR_GIT_URL>

# Step 2: Navigate to the project directory.
cd <YOUR_PROJECT_NAME>

# Step 3: Install the necessary dependencies.
npm install

# Step 4: Start the development server with auto-reloading and an instant preview.
npm run dev
```

## Editing the code

You can edit this project using your preferred IDE or directly in GitHub:

- Open the repository in your IDE and modify files as needed, then commit and push your changes.
- Or, in GitHub:
  - Navigate to the desired file(s).
  - Click the "Edit" button (pencil icon) at the top right of the file view.
  - Make your changes and commit them.

## Deployment

You can deploy this project to any static hosting provider that supports Vite builds (for example, Vercel, Netlify, or GitHub Pages). A typical flow:

```sh
npm run build
```

Then follow your chosen provider's instructions to deploy the contents of the `dist` directory.
